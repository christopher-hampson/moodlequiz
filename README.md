# moodlequiz

Small Python module for populating a LaTeX template with question variants.

LaTeX template utilises moodle.sty to generate an XML file upon compilation in addition to the pdf output that can be used to easily spot-check question variants.
